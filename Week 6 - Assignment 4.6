def computepay(h,r):
    if h > 40:
        return (40*r)+(h-40)*(r*1.5)
    else:
        return h*r

h = float(input("Enter Hours worked:"))
r = float(input("Enter Rate"))

p = computepay(h,r)
print(p)
